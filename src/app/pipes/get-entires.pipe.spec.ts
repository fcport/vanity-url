import { GetEntiresPipe } from './get-entires.pipe';

describe('GetEntiresPipe', () => {
  it('create an instance', () => {
    const pipe = new GetEntiresPipe();
    expect(pipe).toBeTruthy();
  });
});
